#pip install -r requirements.txt
# Python dependencies will be added later
# Medical Multi-Model AI Agent — Python Dependencies
# ====================================================
# Install with:  pip install -r requirements.txt

# ── Database ──────────────────────────────────────────
mysql-connector-python>=8.3.0

# ── AI / ML (uncomment when integrating real models) ──
# tensorflow>=2.15.0
# torch>=2.2.0
# torchvision>=0.17.0
# Pillow>=10.2.0
# numpy>=1.26.0
# scikit-learn>=1.4.0
# opencv-python>=4.9.0

# ── Web Framework (choose one when adding HTTP layer) ──
# flask>=3.0.0
# fastapi>=0.110.0
# uvicorn>=0.27.0

# ── Testing ───────────────────────────────────────────
pytest>=8.1.0
pytest-cov>=5.0.0

# ── Utilities ─────────────────────────────────────────
python-dotenv>=1.0.0
# Medical Multi-Model AI Agent — AI/ML Dependencies
# ====================================================
# Install AI models support with:
#   pip install -r requirements-ai.txt

# ── Core ML Framework ──────────────────────────────
tensorflow>=2.15.0
# Or use: tensorflow-cpu>=2.15.0  (for CPU-only, lighter)

# ── Image Processing ───────────────────────────────
opencv-python>=4.9.0
numpy>=1.26.0

# ── Model Download ─────────────────────────────────
gdown>=5.1.0

# ── Optional: GPU Support ──────────────────────────
# tensorflow-gpu>=2.15.0  (if you have CUDA GPU)

# ── Optional: Alternative frameworks ───────────────
# torch>=2.2.0
# torchvision>=0.17.0
# Pillow>=10.2.0
# scikit-learn>=1.4.0
